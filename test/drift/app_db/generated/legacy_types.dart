enum SourceQualities { high }

enum CloseBehavior { close }

enum LayoutMode { adaptive }

enum Market { US }

enum SearchMode { youtube }

enum ThemeMode { system }

enum AudioSource { youtube }

enum SourceCodecs { weba, m4a }

enum SourceType { youtube }

enum YoutubeClientEngine { youtubeExplode }

enum LyricsCharacterEdge { none }
